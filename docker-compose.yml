services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: whatnot
      POSTGRES_PASSWORD: whatnot
      POSTGRES_DB: sandbox
    ports:
      - "54322:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
